import type { Metadata } from "next";
import { GlobalScreen } from "@/components/admin/website/page-screens";

export const metadata: Metadata = { title: "Footer & tracking" };

export default function Page() {
  return <GlobalScreen />;
}
